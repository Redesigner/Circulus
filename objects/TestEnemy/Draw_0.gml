draw_sprite(sprite_index, image_index, x, y);

draw_text_ext_transformed(x - 4, y - 12, $"HP: {hitPoints}", -1, 200, 0.2, 0.2, 0);

// draw_arrow(x, y, x + velocity.x, y - velocity.y, 2);
// draw_circle(randomizedTargetPosition.x, randomizedTargetPosition.y, 2, false);